PER = {
	dynamic_country_name = { # 
		name = dyn_c_guarded_doamins_of_iran
		adjective = dyn_c_per_adj

		is_main_tag_only = yes
		priority = 1
		
		trigger = {
			exists = scope:actor
			scope:actor ?= {
				NOT = {
					has_variable = je_ir_shattered_borders_broken_sovereignty_var
				}	
			}
		}
	}
	dynamic_country_name = { # Iran
		name = dyn_c_per
		adjective = dyn_c_per_adj

		is_main_tag_only = yes
		priority = 1
		
		trigger = {
			exists = scope:actor
			scope:actor ?= {
				has_variable = je_ir_shattered_borders_broken_sovereignty_var
			}
		}
	}
	
	dynamic_country_name = {
		name = dyn_c_iran
		adjective = dyn_c_iran_adj

		is_main_tag_only = yes
		priority = 100

		trigger = {
			scope:actor = {
				NOT = { is_country_type = unrecognized }
			}
		}
	}
	
}